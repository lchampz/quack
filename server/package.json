{
  "name": "quack-server",
  "version": "1.0.0",
  "description": "Quack WebSocket Server for Render",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "express": "^4.18.2",
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.5",
    "@types/ws": "^8.5.5",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}